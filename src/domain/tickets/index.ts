/**
 * domain/tickets
 * Owns: ticket concepts (ChangeRequest) and lifecycle workflows.
 * Does NOT own: HTTP, auth providers, persistence details.
 * Layer: Domain
 * Trust zone: none
 */

export {};
