/**
 * TicketRepo
 * Owns: persistence access for Ticket.
 * Does NOT own: domain rules, auth, HTTP.
 * Layer: Infrastructure
 * Trust zone: none
 */

export class TicketRepo {
  // TODO (after Prisma models exist): implement CRUD using prisma.ticket
  // Quest 2.2 scope: boundary exists; implementation comes when schema includes Ticket.
}
