/**
 * ClientRepo
 * Owns: persistence access for Client.
 * Does NOT own: domain rules, auth, HTTP.
 * Layer: Infrastructure
 * Trust zone: none
 */

export class ClientRepo {
  // TODO (after Prisma models exist): implement CRUD using prisma.client
  // Quest 2.2 scope: boundary exists; implementation comes when schema includes Client.
}
